<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_forms"
        android:id="@+id/app_bar_forms"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="260dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@drawable/drawer_body_bg"
        app:itemTextColor="@color/drawer_menu_itm"
        app:itemBackground="@drawable/drawer_bkg_color"
        app:itemHorizontalPadding="20dp"
        app:itemTextAppearance="@style/DrawerMenuItemText"
        app:dividerColor="@android:color/transparent"
        app:headerLayout="@layout/nav_header_forms"
        app:menu="@menu/activity_forms_drawer" />

</androidx.drawerlayout.widget.DrawerLayout>
