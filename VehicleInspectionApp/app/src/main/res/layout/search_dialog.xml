<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/search_dialog_bg"
    android:orientation="vertical"
    android:clipChildren="true">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:background="@drawable/dialog_header_top"
        android:gravity="center_vertical"
        android:paddingStart="18dp"
        android:paddingEnd="10dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Select"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/closeDialogBtn"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close_cert"
            android:contentDescription="Close" />

    </LinearLayout>

    <!-- Search field -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:paddingTop="12dp"
        android:paddingBottom="8dp">

        <EditText
            android:id="@+id/searchDialogEditText"
            android:layout_width="match_parent"
            android:layout_height="42dp"
            android:background="@drawable/cert_input_background"
            android:hint="Type to search..."
            android:textColorHint="#8A9BB0"
            android:textColor="#263238"
            android:textSize="13sp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:lines="1"
            android:imeOptions="actionSearch"
            android:inputType="textFilter"
            android:drawableStart="@drawable/ic_search_small"
            android:drawablePadding="8dp" />

    </LinearLayout>

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#EEF2FB"
        android:layout_marginStart="14dp"
        android:layout_marginEnd="14dp" />

    <!-- Results list -->
    <ListView
        android:id="@+id/searchDialogListView"
        android:layout_width="match_parent"
        android:layout_height="320dp"
        android:divider="#EEF2FB"
        android:dividerHeight="1dp"
        android:scrollbars="vertical" />

    <!-- Bottom spacing -->
    <View android:layout_width="match_parent" android:layout_height="8dp" />

</LinearLayout>
